<div class="card mb-5">
    <div class="card-header">{{ comment.user.userName }}
    <span class="cursor-pointer" data-like="{{ path('app_show_comment_like', {'id': comment.id}) }}">
        {% if has_user_liked_comment(app.user, comment) %}
            <i class="fa-solid fa-heart text-danger"></i>
        {% else %}
            <i class="fa-regular fa-heart" aria-hidden="true"></i>
        {% endif %}
        </span>
    </div>
    <div class="card-body">
        <p class="card-text">{{ comment.body}}</p>
    </div>
    <div>
        <p> Nombre de like : <span>{{ comment.likes | length }}</span> </p>
        <p class="d-flex justify-content-end"> Créer le : {{ comment.createdAt |date("m/d/Y") }}</p>
    </div>
</div>
