{% block body %}

<table class="table table-striped">
    <tr>
        <th scope="col">#</th>
        <th scope="col">Titre Principal</th>
        <th scope="col">Photo Central</th>
        <th scope="col">Contenu </th>
        <th scope="col">Lien Instagram (ID)</th>
        <th scope="col">Titre Milieu</th>
        <th scope="col">2eme Contenu</th>
        <th scope="col"></th>
        <th scope="col"></th>
        <th scope="col"></th>
    </tr>
    {% for post in posts %}
        <tbody>
        <tr>
            <th scope="row"> {{ post.id }}</th>
            <td>{{ post.title }}</td>
            <td>{{ post.pathPicPost }}</td>
            <td>{{ post.body}}</td>
            <td>{{ post.LinkSocials }}</td>
            <td>{{ post.TitleCenter }}</td>
            <td>{{ post.BodyCenter }}</td>
{#            <td> <a href="{{ path('app_show_game', {'slug': games.slug}) }}">#}
{#                    <i class="fa-solid fa-eye"></i>#}
{#                </a></td>#}
{#            <td> <a href="{{ path('app_admin_game_edit', {'slug': games.slug}) }}">#}
{#                    <i class="fa-solid fa-edit"></i>#}
{#                </a></td>#}
{#            <td> <a href="{{ path('app_admin_delete_game', {'slug': games.slug}) }}">#}
{#                    <i class="fa-solid fa-trash"></i>#}
{#                </a></td>#}
        </tr>
        <tr>

        </tr>
        </tbody>

    {% endfor %}

</table>

