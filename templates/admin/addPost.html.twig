{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <h1 class="d-flex justify-content-center mt-3">Creer un Post</h1>
        <div class="justify-content-center">
        {{ form_start(addPostForm) }}
        {{ form_end(addPostForm) }}
            </div>
    </div>

{% endblock %}

